# Project 3: Applied Demonstration of an AI Modeling Method
This project demonstrates the limitation of using a linear regression method, specifically logistic regression, that imposes a linear boundary on a non-linear data set. Both a synthetic (2-moons dataset) and real-world dataset (from the UCI repository) will be used to compare the effectiveness and accuracy of the model.
## Section
